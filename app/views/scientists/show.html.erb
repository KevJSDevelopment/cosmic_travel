<h1> <%= @scientist.name %> </h1>
<h3> Field of study: <%= @scientist.field_of_study %></h3>

<ul>
    <% @scientist.planets.each do |planet|%>
        <li> <%= planet.name %> </li>
    <%end%>
</ul>

<%= link_to "Edit #{@scientist.name}", edit_scientist_path(@scientist.id) %>
<%= link_to "Delete #{@scientist.name}", scientist_path(@scientist.id),
    method: "delete"%>